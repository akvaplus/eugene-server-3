<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Management - <%= title %></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body {
            padding-top: 56px;
            padding-bottom: 60px;
        }
        .header-custom {
            background-color: #343a40;
            color: white;
        }
        .footer-custom {
            background-color: #343a40;
            color: white;
            position: fixed;
            bottom: 0;
            width: 100%;
            padding: 10px 0;
        }
        .main-content {
            padding: 20px;
            min-height: calc(100vh - 116px);
        }
    </style>
</head>
<body>
    <header class="header-custom fixed-top">
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark container-fluid">
            <div class="container">
                <a class="navbar-brand" href="/">User Management System</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item">
                            <a class="nav-link <%= activePage === 'users' ? 'active' : '' %>" href="/user">Users</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link <%= activePage === 'new-user' ? 'active' : '' %>" href="/user/new">Add User</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link <%= activePage === 'sessions' ? 'active' : '' %>" href="/session">Sessions</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link <%= activePage === 'new-session' ? 'active' : '' %>" href="/session/new">Add Session</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <div class="container main-content">
        <% if (messages.success) { %>
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <%= messages.success %>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        <% } %>
        
        <% if (messages.error) { %>
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <%= messages.error %>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        <% } %>
        
        <%- body %>
    </div>

    <footer class="footer-custom text-center">
        <div class="container">
            <p>&copy; <%= new Date().getFullYear() %> User Management System. All Rights Reserved.</p>
            <p>Developed with <span style="color: #e25555;">&#9829;</span> by Your Team</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 